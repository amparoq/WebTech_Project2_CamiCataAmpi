<%= form_with(model: requiring_user) do |form| %>
  <% if requiring_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(requiring_user.errors.count, "error") %> prohibited this requiring_user from being saved:</h2>

      <ul>
        <% requiring_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
